The user intends to adapt an element to:
"{{ ctx.adaptation }}"

Generate an image based on this synthesized prompt:
"{{ ctx.master_prompt }}"

The SECOND attached image is the front view already generated for this subject. Adapt the FIRST attached image (the subject) according to the style references as a strict dead-on back view. Maintain perfect visual consistency with the already-generated front view — same colors, textures, proportions, and applied style. Do not stylize beyond the given style references.
The subject must be fully in-frame isolated against a seamless pure white background.
