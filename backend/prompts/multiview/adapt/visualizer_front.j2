The user intends to adapt an element to:
"{{ ctx.adaptation }}"

Generate an image based on this synthesized prompt:
"{{ ctx.master_prompt }}"

Adapt the FIRST attached image (the subject) according to the style references as a strict dead-on front view. Preserve the subject's identity, structure, and proportions exactly while applying the style from the references. Reference fidelity takes priority over stylistic interpretation.
Do not stylize beyond the given style references.
The subject must be fully in-frame isolated against a seamless pure white background.
