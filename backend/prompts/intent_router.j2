User prompt
The user intends to generate:
"{{ ctx.prompt }}"

Item to evaluate
{% if ctx.mode == "token" %}
Element:
- Type: {{ ctx.item.type }}
- Title: "{{ ctx.item.title }}"
- Description: "{{ ctx.item.description }}"
{% if ctx.cluster_context %}
- Part of cluster: "{{ ctx.cluster_context }}"

{% elif ctx.mode == "cluster" %}
Cluster:
- Title: "{{ ctx.item.title }}"
- Description: "{{ ctx.item.description }}"
- Contains {{ ctx.item.element_count }} design elements

{% endif %}
