User's generation intent:
"{{ ctx.prompt }}"

Moodboard analysis (ordered by relevance):
{% for cluster in ctx.clusters %}

Cluster: "{{ cluster.title }}"
Description: {{ cluster.description }}
{% if cluster.elements %}
Key elements:
{% for elem in cluster.elements %}
- {{ elem.type | capitalize }}: "{{ elem.title }}" ({{ elem.weight }}%) - {{ elem.description }}
{% endfor %}
{% endif %}
{% endfor %}
Synthesize these into a single, concrete generation prompt with the image references. Focus on visual qualities — materials, colors, textures, and forms — that translate directly to imagery. Be specific and descriptive; avoid abstract concepts.
The subject must be fully in-frame isolated against a seamless pure white background.