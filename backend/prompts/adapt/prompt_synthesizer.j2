User prompt
The user intends to adapt:
"{{ ctx.subject }}"
to the style specified by:
"{{ ctx.adaptation }}"

Moodboard analysis (ordered by relevance):
{% for cluster in ctx.clusters %}

Cluster: "{{ cluster.title }}"
Description: {{ cluster.description }}
{% if cluster.elements %}
Key elements:
{% for elem in cluster.elements %}
- {{ elem.type | capitalize }}: "{{ elem.title }}" ({{ elem.weight }}%) - {{ elem.description }}
{% endfor %}
{% endif %}
{% endfor %}

Synthesize these into a single, concrete adaptation prompt with the image references. Focus on visual qualities — materials, colors, textures, and forms — that translate directly to imagery. Describe how the subject should be transformed, not just what the style is. Be specific; avoid abstract concepts.
The subject must be fully in-frame isolated against a seamless pure white background.